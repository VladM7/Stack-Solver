<Page x:Class="Stack_Solver.Views.Pages.PalletBuilderPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Stack_Solver.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:conv="clr-namespace:Stack_Solver.Converters"
    xmlns:behaviors="clr-namespace:Stack_Solver.Helpers.Behaviors"
    Title="PalletBuilderPage"
    d:DataContext="{d:DesignInstance local:PalletBuilderPage,
                                IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Page.Resources>
        <conv:BooleanNegationConverter x:Key="BooleanNegationConverter" />
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Column="0" Grid.Row="0">
            <ui:Button x:Name="generateButton" Content="Generate" Command="{Binding ViewModel.LayerAnalyzer.GenerateCommand}" IsEnabled="{Binding ViewModel.LayerAnalyzer.IsGenerating, Converter={StaticResource BooleanNegationConverter}}" Grid.Column="0" Grid.Row="0" VerticalAlignment="Top" Icon="{ui:SymbolIcon ArrowTurnDownRight48}" Appearance="Primary"/>
            <ui:Button Content="Cancel" Command="{Binding ViewModel.LayerAnalyzer.CancelCommand}" Icon="{ui:SymbolIcon DismissCircle24}" Margin="8,0,0,0" VerticalAlignment="Top" IsEnabled="{Binding ViewModel.LayerAnalyzer.IsGenerating}"/>
            <ui:Button x:Name="topHelpButton" Icon="{ui:SymbolIcon Question24}" Margin="8,0,0,0" VerticalAlignment="Top" Click="TopHelpButton_Click" Cursor="Help">
                <WrapPanel>
                    <ui:TextBlock Text="Help"/>
                    <ui:Flyout x:Name="helpFlyout" Placement="Mouse" Width="0" Height="0" MinWidth="0" MinHeight="0" VerticalAlignment="Top">
                        <ui:TextBlock>
                            <Run Text="Here you can adjust different parameters and see the results."/>
                            <LineBreak/>
                            <LineBreak/>
                            <Run Text="1 - Set the pallet dimensions manually or by selecting a pallet type."/>
                            <LineBreak/>
                            <Run Text="2 - Check the box next to each SKU you want to include, then set its quantity."/>
                            <LineBreak/>
                            <Run Text="(unchecked SKUs are ignored regardless of quantity)"/>
                            <LineBreak/>
                            <Run Text="You can add more SKU types (or edit them) in the "/>
                            <Run Text="Library " FontWeight="Bold"/>
                            <Run Text="page." />
                            <LineBreak/>
                            <Run Text="3 - Change the layer generation and stacking options."/>
                            <LineBreak/>
                            <Run Text="4 - Click"/>
                            <Run Text=" Generate" FontWeight="Bold"/>
                            <Run Text=" and see the results in the analyzer pages."/>
                        </ui:TextBlock>
                    </ui:Flyout>
                </WrapPanel>
            </ui:Button>
        </StackPanel>
        <ProgressBar Grid.Row ="1" Height="5" Margin="0,4,0,0" IsIndeterminate="True" Visibility="{Binding ViewModel.LayerAnalyzer.IsGenerating, Converter={StaticResource BoolToVisibilityConverter}}"/>
        <TabControl Grid.Row="2" Grid.Column="1" Margin="0,8,0,0">
            <TabItem Header="Settings" HorizontalAlignment="Left" Width="150">
                <ScrollViewer x:Name="SettingsScrollViewer" Margin="0,8,0,0" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="SettingsStackPanel" Orientation="Vertical" behaviors:ParentScrollBehavior.Enabled="True">
                        <ui:CardExpander Header="Pallet dimensions">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Label>Length</Label>
                                <ui:TextBox x:Name="palletLengthTextBox" Text="{Binding ViewModel.Settings.PalletLength, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" Width="80" Grid.Row="1"/>
                                <Label Grid.Column="1">Width</Label>
                                <ui:TextBox x:Name="palletWidthTextBox" Text="{Binding ViewModel.Settings.PalletWidth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" Width="80" Grid.Row="1" Grid.Column="1"/>
                                <Label Grid.Column="2">Height</Label>
                                <ui:TextBox x:Name="palletHeightTextBox" Text="{Binding ViewModel.Settings.PalletHeight, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" Width="80" Grid.Row="1" Grid.Column="2"/>

                                <ui:DataGrid Grid.Row="2" Grid.ColumnSpan="3"
                         ItemsSource="{Binding ViewModel.Settings.CommonPalletsInternational}"
                         SelectedItem="{Binding ViewModel.Settings.SelectedInternationalPallet, Mode=TwoWay}"
                         CanUserAddRows="False"
                         CanUserDeleteRows="False"
                         AutoGenerateColumns="False"
                         IsReadOnly="True"
                         Margin="0,10,0,0"
                         HeadersVisibility="Column"
                         ScrollViewer.CanContentScroll="False"
                         ScrollViewer.VerticalScrollBarVisibility="Disabled">
                                    <ui:DataGrid.Columns>
                                        <DataGridTextColumn Header="EUR/Intl. Pallets" Binding="{Binding Name}" Width="Auto"/>
                                        <DataGridTextColumn Header="Length (cm)" Binding="{Binding Length}" Width="Auto"/>
                                        <DataGridTextColumn Header="Width (cm)" Binding="{Binding Width}" Width="Auto"/>
                                    </ui:DataGrid.Columns>
                                </ui:DataGrid>
                                <ui:DataGrid Grid.Row="3" Grid.ColumnSpan="3"
                 ItemsSource="{Binding ViewModel.Settings.CommonPalletsAmerica}"
                 SelectedItem="{Binding ViewModel.Settings.SelectedAmericanPallet, Mode=TwoWay}"
                 CanUserAddRows="False"
                 CanUserDeleteRows="False"
                 AutoGenerateColumns="False"
                 IsReadOnly="True"
                 Margin="0,10,0,0"
                 HeadersVisibility="Column"
                 ScrollViewer.CanContentScroll="False"
                 ScrollViewer.VerticalScrollBarVisibility="Disabled">
                                    <ui:DataGrid.Columns>
                                        <DataGridTextColumn Header="American Pallets" Binding="{Binding Name}" Width="Auto"/>
                                        <DataGridTextColumn Header="Length (inch)" Binding="{Binding Length}" Width="Auto"/>
                                        <DataGridTextColumn Header="Width (inch)" Binding="{Binding Width}" Width="Auto"/>
                                    </ui:DataGrid.Columns>
                                </ui:DataGrid>
                            </Grid>
                        </ui:CardExpander>
                        <ui:CardExpander Header="SKU selection">
                            <StackPanel Orientation="Vertical" Margin="0,0,0,0">
                                <ui:TextBlock Text="Select the SKUs to include in the pallet. Don't forget to also set the quantity." Margin="0,0,0,5"/>
                                <ui:DataGrid
            x:Name="SkuSelectionGrid"
            ItemsSource="{Binding ViewModel.Settings.Skus, Mode=TwoWay}"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            AutoGenerateColumns="False"
            CellEditEnding="SkuSelectionGrid_CellEditEnding"
            ScrollViewer.CanContentScroll="False"
            ScrollViewer.VerticalScrollBarVisibility="Disabled">
                                    <ui:DataGrid.Columns>
                                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" IsReadOnly="True" Width="Auto"/>
                                        <DataGridTemplateColumn Header="Dimensions (cm)" Width="150" IsReadOnly="True">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock>
                                                        <TextBlock.Text>
                                                            <MultiBinding StringFormat="{}{0} x {1} x {2}">
                                                                <Binding Path="Length"/>
                                                                <Binding Path="Width"/>
                                                                <Binding Path="Height"/>
                                                            </MultiBinding>
                                                        </TextBlock.Text>
                                                    </TextBlock>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn Header="Weight" Binding="{Binding Weight}" IsReadOnly="True" Width="Auto"/>
                                        <DataGridTextColumn Header="Qty" Binding="{Binding Quantity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="Auto"/>
                                        <DataGridTemplateColumn Width="Auto">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                              HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"
                                                              Click="SkuCheckBox_Click"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </ui:DataGrid.Columns>
                                </ui:DataGrid>
                            </StackPanel>
                        </ui:CardExpander>
                        <ui:CardExpander Header="Layer generation options">
                            <StackPanel Orientation="Vertical">
                                <ui:CardControl Header="Number of BLF attempts">
                                    <ui:NumberBox x:Name="maxBLFAttempts"
                                          Value="{Binding ViewModel.Settings.BlfAttempts, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          Minimum="0" SmallChange="10" LargeChange="10"/>
                                </ui:CardControl>
                                <ui:CardControl Header="Use CP-SAT layer generation">
                                    <ui:ToggleSwitch x:Name="cpsatToggleSwitch" IsChecked="{Binding ViewModel.Settings.UseCpsat, Mode=TwoWay}"/>
                                </ui:CardControl>
                                <ui:CardControl Header="Maximum CP-SAT candidate placements">
                                    <ui:NumberBox x:Name="maxCpsatCandidatesNumberBox"
                                                  Value="{Binding ViewModel.Settings.MaxCpsatCandidates, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  Minimum="1"
                                                  IsEnabled="{Binding ViewModel.Settings.UseCpsat}" SmallChange="100" LargeChange="1000"/>
                                </ui:CardControl>
                                <ui:CardControl Header="CP-SAT solver time limit (seconds)">
                                    <ui:NumberBox x:Name="solverTimeLimitNumberBox"
                                                  Value="{Binding ViewModel.Settings.SolverTimeLimit, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  Minimum="1"
                                                  IsEnabled="{Binding ViewModel.Settings.UseCpsat}" />
                                </ui:CardControl>
                            </StackPanel>
                        </ui:CardExpander>
                        <ui:CardExpander Header="Layer stacking options" Margin="0,0,0,20">
                            <StackPanel>
                                <ui:CardControl Header="Maximum loaded pallet height">
                                    <ui:NumberBox HorizontalAlignment="Left" Value="{Binding ViewModel.Settings.MaxStackHeight, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Minimum="1" />
                                </ui:CardControl>
                                <ui:CardControl Header="Maximum loaded pallet weight">
                                    <ui:NumberBox HorizontalAlignment="Left" Value="{Binding ViewModel.Settings.MaxStackWeight, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Minimum="1" />
                                </ui:CardControl>
                                <ui:CardControl Header="Maximum SKU overhang">
                                    <ui:NumberBox HorizontalAlignment="Left"
                                                  Value="{Binding ViewModel.Settings.MaxSkuOverhang, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  Minimum="0"
                                                  SmallChange="10"
                                                  LargeChange="100" />
                                </ui:CardControl>
                            </StackPanel>
                        </ui:CardExpander>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Layer Analyzer" HorizontalAlignment="Left" Width="151">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,5" Grid.ColumnSpan="2">
                        <ui:SymbolIcon Symbol="Info24" VerticalAlignment="Center"/>
                        <ui:TextBlock Text="{Binding ViewModel.LayerAnalyzer.LayerGenStats}" Margin="5,0,0,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.ColumnSpan="2" Margin="0,5,0,0">
                        <ui:Button Content="Save Report" Icon="{ui:SymbolIcon Save24}" Height="38" IsEnabled="False" />
                        <ComboBox x:Name="layersComboBox" Margin="10,0,0,0" VerticalAlignment="Bottom" ItemsSource="{Binding ViewModel.LayerAnalyzer.Layers}" SelectedItem="{Binding ViewModel.LayerAnalyzer.SelectedLayer, Mode=TwoWay}" DisplayMemberPath="Name" IsEnabled="{Binding ViewModel.LayerAnalyzer.HasLayers}"/>
                        <ui:TextBlock Margin="10,0,0,0" Text="{Binding ViewModel.LayerAnalyzer.SelectedItemInfo}" VerticalAlignment="Center" FontFamily="Cascadia Code"/>
                    </StackPanel>
                    <Viewport3D x:Name="MainViewPort" Grid.Row="2" Grid.Column="1"
                        xmlns:behaviors="clr-namespace:Stack_Solver.Helpers.Rendering"
                        behaviors:ViewportBehaviors.ZoomCommand="{Binding ViewModel.LayerAnalyzer.ZoomCommand}"
                        behaviors:ViewportBehaviors.BeginPanCommand="{Binding ViewModel.LayerAnalyzer.BeginPanCommand}"
                        behaviors:ViewportBehaviors.PanCommand="{Binding ViewModel.LayerAnalyzer.PanCommand}">
                        <Viewport3D.Camera>
                            <PerspectiveCamera x:Name="MainPerspectiveCamera" LookDirection="-1,-1,-1" UpDirection="0,1,0" Position="150,150,150" FieldOfView="60"/>
                        </Viewport3D.Camera>
                        <ModelVisual3D x:Name="MainLight">
                            <ModelVisual3D.Content>
                                <DirectionalLight Color="White" Direction="-2,-3,-1"/>
                            </ModelVisual3D.Content>
                        </ModelVisual3D>
                        <ModelVisual3D Content="{Binding ViewModel.LayerAnalyzer.Scene}" />
                    </Viewport3D>
                    <GridSplitter Grid.Row="2" Grid.Column="0" Width="3" HorizontalAlignment="Right" Height="200"/>
                    <Border Grid.Row="2" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4" Padding="6" Margin="0,7,10,7">
                        <StackPanel>
                            <TextBlock Text="Layer Details" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <ui:TextBlock Text="{Binding ViewModel.LayerAnalyzer.OutputText}" FontFamily="Cascadia Code" FontSize="14" TextWrapping="Wrap" Margin="0,10,0,10"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
            <TabItem Header="Pallet Analyzer" HorizontalAlignment="Left" Width="150">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.ColumnSpan="2" Margin="0,5,0,5">
                        <ui:Button Content="Save Report" Icon="{ui:SymbolIcon Save24}" IsEnabled="False"/>
                    </StackPanel>
                    <Border Grid.Row="2" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4" Padding="6" Margin="0,7,10,7">
                        <StackPanel>
                            <TextBlock Text="Pallet Details" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <ui:TextBlock Text="{Binding ViewModel.PalletAnalyzer.OutputText}" FontFamily="Cascadia Code" FontSize="14" TextWrapping="Wrap" Margin="0,10,0,10"/>
                        </StackPanel>
                    </Border>
                    <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Vertical" Margin="0,7,10,7">
                        <Border BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4" Margin="0,0,0,6">
                            <ui:DataGrid AutoGenerateColumns="False"
                                         CanUserAddRows="False"
                                         CanUserDeleteRows="False"
                                         IsReadOnly="True"
                                         HeadersVisibility="Column"
                                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                                         BorderThickness="0"
                                         ItemsSource="{Binding ViewModel.PalletAnalyzer.Assignments}"
                                         SelectedItem="{Binding ViewModel.PalletAnalyzer.SelectedAssignment, Mode=TwoWay}">
                                <ui:DataGrid.Columns>
                                    <DataGridTextColumn Header="Pallet" Binding="{Binding Name}"/>
                                    <DataGridTextColumn Header="Times Used" Binding="{Binding Count}"/>
                                    <DataGridTextColumn Header="# Layers" Binding="{Binding Template.Layers.Count}"/>
                                    <DataGridTextColumn Header="# Boxes" Binding="{Binding Template.TotalBoxCount}"/>
                                    <DataGridTextColumn Header="Contents" Binding="{Binding Contents}"/>
                                    <DataGridTextColumn Header="Load Dim." Binding="{Binding LoadDimensions}"/>
                                    <DataGridTextColumn Header="Wght" Binding="{Binding Weight}"/>
                                    <DataGridTextColumn Header="Util" Binding="{Binding Efficiency}"/>
                                </ui:DataGrid.Columns>
                            </ui:DataGrid>
                        </Border>
                        <Border BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4">
                            <ui:DataGrid AutoGenerateColumns="False"
                                         CanUserAddRows="False"
                                         CanUserDeleteRows="False"
                                         IsReadOnly="True"
                                         HeadersVisibility="Column"
                                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                                         BorderThickness="0"
                                         ItemsSource="{Binding ViewModel.PalletAnalyzer.SelectedLayerTypes}">
                                <ui:DataGrid.Columns>
                                    <DataGridTextColumn Header="Layer" Binding="{Binding Name}"/>
                                    <DataGridTextColumn Header="Times Used" Binding="{Binding Count}"/>
                                    <DataGridTextColumn Header="Contents" Binding="{Binding Contents}"/>
                                    <DataGridTextColumn Header="Util" Binding="{Binding Utilization}"/>
                                </ui:DataGrid.Columns>
                            </ui:DataGrid>
                        </Border>
                    </StackPanel>
                    <GridSplitter Grid.Row="1" Grid.RowSpan="2" Grid.Column="0" Width="3" HorizontalAlignment="Right" Height="200"/>
                    <GridSplitter Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Height="3" VerticalAlignment="Top" HorizontalAlignment="Center" Width="200" ResizeDirection="Rows"/>
                    <StackPanel Orientation="Vertical" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,7,0,0">
                        <ui:DataGrid AutoGenerateColumns="False" FontSize="14"
                                     CanUserAddRows="False"
                                     CanUserDeleteRows="False"
                                     IsReadOnly="True"
                                     HeadersVisibility="Column"
                                     ItemsSource="{Binding ViewModel.PalletAnalyzer.Solutions}"
                                     SelectedItem="{Binding ViewModel.PalletAnalyzer.SelectedSolution, Mode=TwoWay}">
                            <ui:DataGrid.Columns>
                                <DataGridTextColumn Header="Sol. #" Binding="{Binding Number}"/>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                                <DataGridTextColumn Header="# Pallets" Binding="{Binding TotalPallets}"/>
                                <DataGridTextColumn Header="# Pallet Types" Binding="{Binding PalletTypes}"/>
                                <DataGridTextColumn Header="Total Items Packed" Binding="{Binding TotalItemsPacked}"/>
                                <DataGridTextColumn Header="Eff" Binding="{Binding Efficiency}"/>
                            </ui:DataGrid.Columns>
                        </ui:DataGrid>
                    </StackPanel>

                    <Viewport3D x:Name="PalletViewPort" Grid.Row="0" Grid.RowSpan="3" Grid.Column="1"
                                xmlns:rendering="clr-namespace:Stack_Solver.Helpers.Rendering"
                                rendering:ViewportBehaviors.ZoomCommand="{Binding ViewModel.PalletAnalyzer.ZoomCommand}"
                                rendering:ViewportBehaviors.BeginPanCommand="{Binding ViewModel.PalletAnalyzer.BeginPanCommand}"
                                rendering:ViewportBehaviors.PanCommand="{Binding ViewModel.PalletAnalyzer.PanCommand}">
                        <Viewport3D.Camera>
                            <PerspectiveCamera x:Name="PalletPerspectiveCamera"
                                               LookDirection="-1,-1,-1" UpDirection="0,1,0"
                                               Position="150,150,150" FieldOfView="60"/>
                        </Viewport3D.Camera>
                        <ModelVisual3D>
                            <ModelVisual3D.Content>
                                <DirectionalLight Color="White" Direction="-2,-3,-1"/>
                            </ModelVisual3D.Content>
                        </ModelVisual3D>
                        <ModelVisual3D Content="{Binding ViewModel.PalletAnalyzer.Scene}"/>
                    </Viewport3D>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Page>
